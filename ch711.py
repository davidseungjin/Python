user_text = input('Enter a string: ')
print()

first_half = user_text[:len(user_text)//2]
last_half = user_text[len(user_text)//2:]

print('The first half', first_half)
print('The second half', last_half)

